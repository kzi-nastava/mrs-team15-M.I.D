import { Component } from '@angular/core';
import { DriverActivationForm } from '../../components/driver-activation-form/driver-activation-form';

@Component({
  selector: 'app-driver-activation',
  imports: [DriverActivationForm],
  templateUrl: './driver-activation.html',
  styleUrl: './driver-activation.css',
})
export class DriverActivation {

}
