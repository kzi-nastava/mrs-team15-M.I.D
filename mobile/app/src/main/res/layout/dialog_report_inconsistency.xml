<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/report_inconsistency_title"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"
        android:textColor="?android:attr/textColorPrimary" />

    <!-- Subtitle -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/report_inconsistency_subtitle"
        android:textSize="14sp"
        android:layout_marginBottom="20dp"
        android:textColor="?android:attr/textColorSecondary" />

    <!-- Input field -->
    <EditText
        android:id="@+id/inconsistencyEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/report_inconsistency_hint"
        android:inputType="textMultiLine|textCapSentences"
        style="@style/TextInputLong"
        android:layout_marginBottom="24dp"
        android:gravity="top|start">

    </EditText>

    <!-- Character counter -->
    <TextView
        android:id="@+id/characterCounter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0/300"
        android:textSize="12sp"
        android:layout_gravity="end"
        android:layout_marginBottom="20dp"
        android:textColor="?android:attr/textColorSecondary" />

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/cancelButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cancel"
            android:layout_marginEnd="8dp"
            style="@style/ButtonSecondary" />

        <Button
            android:id="@+id/submitButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/report_inconsistency_submit"
            android:enabled="false"
            style="@style/ButtonPrimary"/>

    </LinearLayout>

</LinearLayout>
